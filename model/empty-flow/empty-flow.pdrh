N(0, 1) x;
N(0, 1) y;
N(0, 1) z;
[0, 1] time;
[0, 1] t;

{ mode 1;
  
  flow:
     d/dt[t] = 1.0;
  jump:
  	(t >= 1) ==> @1 (t' = t);
}

init:
@1     (t = 0);

goal:
@1    (and (x >= 0.0) (y >= 0.0) (z >= 0.0));

