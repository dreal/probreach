#define K 1.0

[0, 5] time;
[0, 1000] tau;


{ mode 1;
  invt:
	(and (x >= 18));
  flow:
	d/dt[x] = - x * K;
	d/dt[tau] = 10.0;
	d/dt[T0] = 0.0;
  jump:
        (x <= 18) ==> @2 (and (x' = x) (tau' = tau));
}
{ mode 2;
  invt:
	(and (x <= 22));
  flow:
	d/dt[x] = - K * (x - 30);
	d/dt[tau] = 10.0;
	d/dt[T0] = 0.0;
  jump:
        (x >= 22) ==> @1 (and (x' = x) (tau' = tau));
}
init:
@1	(and (x >= x_a) (x <= x_b) (tau = 0));

goal:
@2	(and (or (x < 19.9) (x > 20.1)) (tau = 18));
